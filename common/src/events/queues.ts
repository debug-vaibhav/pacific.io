export enum Queues {
    Audit = 'queue.audit',
    Core = 'queue.core',
    Logging = 'queue.logging',
    Monitor = 'queue.monitor',
    Notification = 'queue.notification',
    User = 'queue.user',
}
