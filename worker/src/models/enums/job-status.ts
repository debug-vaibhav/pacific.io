export enum JobStatus {
    READY = 'ready',
    RUNNING = 'running',
    PAUSED = 'paused',
    STOPPED = 'stopped',
    KILLED = 'killed',
    RESUMED = 'resumed',
    CANCELLED = 'cancelled',
    SCHEDULED = 'scheduled',
    SKIPPED = 'skipped',
    PICKED = 'picked',
}
